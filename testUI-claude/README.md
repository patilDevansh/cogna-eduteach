# testUI-claude — local UI preview only

Standalone Claude UI mock. **Not** part of the eduTeach / Cogna monorepo workspace.

```bash
cd testUI-claude
npm install
npm run dev -- -p 3100
```

Open http://localhost:3100

Demo student code: **MATH42**. Parent login accepts any email locally.
Works offline without the API (local orchestrator + content bank).
