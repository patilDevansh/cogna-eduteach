# MVP 8.0 Content Review Checklist — Exponents (algebra slice)

> Everything ships `PENDING_REVIEW`; nothing here should move to `APPROVED` without both sign-offs below.

## Sign-off

| Reviewer | Unit | Date | Signature |
|---|---|---|---|
| TBD (math) | exponents-algebra | TBD | TBD |
| TBD (product/copy) | exponents-algebra | TBD | TBD |

## Per question

- [ ] Exponent arithmetic is correct (re-derive by hand or script, not visual inspection)
- [ ] Answers never show `x^0` (must be `1`) or `x^1` (must be `x`) in a simplified answer
- [ ] Negative-exponent answers consistently offered in both `1/x^n` and `x^-n` form where applicable
- [ ] `misconceptionAnswerPatterns` wrong answers are genuinely reachable by the described error, and never equal the correct answer
- [ ] `unitId` is `"exponents-algebra"`

## Freeze

- [ ] No ID collisions with `P#_*`, `C#_*`, `SE_*`, `ID_*`, or `FAC_*` concept/question IDs
- [ ] `EXPONENT_LAW_MISAPPLICATION` and `NEGATIVE_EXPONENT_SIGN_FLIP` explanations exist in `docs/mvp-1.0/content/explanations/templates.json` before any question targeting them goes `APPROVED`
